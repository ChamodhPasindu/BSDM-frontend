export interface IBatch {
  batchCode: string;
  manufactureDate: string;
  expiryDate: string;
  usableDays: number;
  remainingQuantity: number;
  // statusCode: string;
  // warehouseLocation: string;
  // createdAt: string;
  // createdBy: number;
}
